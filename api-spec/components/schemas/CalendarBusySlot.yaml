type: object
properties:
  start:
    type: string
    format: date-time
    description: Start of the busy slot
  end:
    type: string
    format: date-time
    description: End of the busy slot
required:
  - start
  - end
