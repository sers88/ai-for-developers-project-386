type: object
properties:
  connected:
    type: boolean
    description: Whether Google Calendar is connected
  email:
    type: string
    format: email
    description: Google Calendar email address
required:
  - connected
